# Server Configuration
PORT=3001
NODE_ENV=development

# CORS Configuration
FRONTEND_URL=http://localhost:8080

# Webhook Configuration (for ngrok or public URL)
# This is the public URL where DMS can reach your backend
WEBHOOK_BASE_URL=http://localhost:3001

# Logging
LOG_LEVEL=debug

# NOTE: DMS credentials (JWT_SECRET, CHANNEL_ID, API_URL) are now provided
# dynamically through the frontend configuration panel. No need to hardcode them here!