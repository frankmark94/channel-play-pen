# Production Environment Variables for Render.com
# These will be set in the Render dashboard

# Server Configuration
NODE_ENV=production
PORT=10000

# CORS Configuration (Set in Render dashboard to your frontend URL)
FRONTEND_URL=https://your-frontend-service.onrender.com

# Webhook Configuration (Set in Render dashboard to your backend URL)
# This is where DMS will send webhook notifications
WEBHOOK_BASE_URL=https://your-backend-service.onrender.com

# Logging
LOG_LEVEL=info

# ==================================================================================
# NOTE: DMS credentials are NO LONGER set here!
# ==================================================================================
# JWT_SECRET, CHANNEL_ID, API_URL, and CUSTOMER_ID are now provided dynamically
# through the frontend configuration panel at runtime.
#
# Users enter their Pega DMS credentials directly in the web UI, allowing a single
# deployment to serve multiple users and organizations without redeployment.
# ==================================================================================