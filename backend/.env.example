# Server Configuration
PORT=3001
NODE_ENV=development

# CORS Configuration
FRONTEND_URL=http://localhost:8080

# Webhook Configuration (for ngrok or public URL)
# This is the public URL where DMS can reach your backend
# For local development with ngrok: https://your-subdomain.ngrok.io
# For production on Render: https://your-app.onrender.com
WEBHOOK_BASE_URL=https://your-ngrok-url.com

# Logging
LOG_LEVEL=debug

# NOTE: DMS credentials (JWT_SECRET, CHANNEL_ID, API_URL, CUSTOMER_ID) are now
# provided dynamically through the frontend configuration panel at runtime.
# Users enter their credentials directly in the UI - no need to set them here!